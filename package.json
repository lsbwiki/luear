{
  "main": "server.js",
  "scripts": {
    "preinstall": "curl -L -o xray.zip https://github.com/XTLS/Xray-core/releases/latest/download/Xray-linux-64.zip && unzip -o xray.zip && chmod +x xray",
    "start": "node server.js"
  }
}
